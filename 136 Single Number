class Solution:
    def singleNumber(self, nums: List[int]) -> int:
        count = 0
        for i in nums:
            count = i ^ count       #Performing xor operation 2 ^ 2 = 0
        return count
